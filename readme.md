# Group Builder
__Lorem Ipsum__



### Dev Notes:
- In projects root `pyreverse -o png -p GroupBuilder .` to regenerate UML.
- In `./docs` run  `sphinx-build -M html source/ build/` to regenerate documentation.
- To build wxPython on archlinux use `LD_LIBRARY_PATH='$(pkg-config --cflags gtk+-3.0)' pip install wxPython` (assuming all dependencies are present).